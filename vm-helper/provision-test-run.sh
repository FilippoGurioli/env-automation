#!/bin/bash

#TODO: create a vm that has only done the bootstrap phase using vm, ssh and 123 as inputs.
#TODO: then add ssh compatibility.
#TODO: then create an image from that vm.
#TODO: then here exploit that image to launch the provision phase with interactive ssh log.